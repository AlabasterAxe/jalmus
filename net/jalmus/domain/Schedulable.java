package net.jalmus.domain;

/* This is the interface used to declare
 * something as schedulable in a measure which means
 * that the note will occur at a given time.
 */
public interface Schedulable {}
